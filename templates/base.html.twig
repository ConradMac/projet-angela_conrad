<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>
      {% block title %}

      {% endblock %}- Agence de com/ Dev
    </title>
    <link rel="shortcut icon"
      href="{{ '/images/' ~ 'koko.png' }}"
      type="image/x-icon"
      sizes="16x16" />
    <link rel="stylesheet" href="{{ '/asset/' ~ 'fad.js' }}" />

    <!-- Compiled and minified CSS -->

    <!-- Let browser know website is optimized for mobile -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}

    {% block stylesheets %}
      {{ encore_entry_link_tags('app') }} {# this will include app.css #}
    {% endblock %}

    {% block javascripts %}
      {{ encore_entry_script_tags('app') }}
    {% endblock %}
  </head>
  <header>
    {{ include('_parties/_nav.html.twig') }}

    {{ include('_parties/_elementsjs.html.twig') }}
  </header>
  <body>
    {# on fait un include pour qu'il soit sur toutes les pages du site. a cet endroit. #}

    {% block body %}
      {# ajouter le contenu de la page ou autre. #}
    {% endblock %}

    {{ include('_parties/_footer.html.twig') }}
    <!-- Compiled and minified JavaScript -->
  </body>
</html>
